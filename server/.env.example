PORT=5000
# Optional MongoDB Atlas connection URI (if omitted, falls back to in-memory store)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/cleancity?retryWrites=true&w=majority
NODE_ENV=production

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=np9257145@gmail.com
SMTP_PASS=skqc zwkv xemm iybz
SMTP_FROM="CleanCity AI" <np9257145@gmail.com>